# 测试证据目录

原始 HTTP 响应、浏览器旅程、JSON/JSONL 台账和部署报告可能包含测试载荷、临时地址或运行环境元数据，因此默认被 `.gitignore` 排除，不发布到公共仓库。

公开 PR 只应提交：

- 合成数据得到的脱敏摘要；
- 可复现的测试命令与退出状态；
- 代码提交 SHA 或文件哈希；
- 不含主机、账号、密钥和个人信息的最小失败证据。

完整测试方法见 [`docs/TESTING.md`](../docs/TESTING.md)。
