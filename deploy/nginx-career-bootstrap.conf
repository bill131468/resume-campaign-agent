server {
    listen 80;
    listen [::]:80;
    server_name resume-agent.example.com;

    location ^~ /.well-known/acme-challenge/ {
        root /var/www/resume-agent-acme;
        default_type text/plain;
    }

    location / {
        return 308 https://$host$request_uri;
    }
}
