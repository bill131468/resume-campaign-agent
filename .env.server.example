# 生产环境优先使用云平台或容器编排器的密钥管理器注入这些变量。
# 如果使用本文件的副本 .env.server，请限制为仅部署账号可读，且绝不能提交版本库。
LLM_PROVIDER=openai-compatible
LLM_API_KEY=
LLM_BASE_URL=
LLM_MODEL=
REQUEST_TIMEOUT_SECONDS=20
AI_RANKING_TIMEOUT_SECONDS=45

# Docker Compose 只把服务发布到远程服务器回环地址。
AGENT_PORT=18010
