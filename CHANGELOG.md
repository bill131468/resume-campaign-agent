# Changelog

本项目遵循语义化版本。浏览器扩展因发布节奏独立维护单独版本号。

## [Unreleased]

- 预留持久数据库、身份认证、稳定域名和训练反馈闭环。
- 修复工作台丢弃 FastAPI 字段级校验信息的问题；现在会显示中文字段路径并定位对应输入框，不再把所有 `422` 误报为岗位 URL 或 JD 缺失。

## [0.2.2] - 2026-08-06

### Added

- 仅本机运行的源码交付包、Windows 安装/启动/验证脚本和包内安全检查。
- 面向人类使用者的本地使用手册，以及面向 AI / Codex 维护者的结构化交接手册。

### Fixed

- 扩展 0.7.1 不再从 service worker 请求单站点权限；权限弹窗由侧边栏的直接用户点击触发，修复 `This function must be called during a user gesture`。
- 工作台的扩展版本提示从 0.6.0 更正为 0.7.1。

## [0.2.1] - 2026-08-05

### Fixed

- 正式部署默认不再载入合成简历或虚构岗位数据。
- 测试 fixture 路由改为 `ENABLE_TEST_FIXTURES=true` 才启用；生产模式下模拟回执及其 `.html` 路径均返回 `404`。
- 浏览器扩展 0.7.0 删除“本机全流程招聘演练”入口。

### Changed

- 健康接口明确返回 `deployment_mode=production` 与 `delivery_mode=per_application_authorized`。

## [0.2.0] - 2026-08-05

### Added

- JD 解析、岗位匹配、一岗一简历、事实审计、投递排序与渠道去重。
- 投递看板、提醒、门户预检、断点恢复、面试准备、模拟面试和漏斗分析。
- 招聘风险检查、事实证据库和临时加密保险箱。
- 通用简历六维审核与建议式优化。
- 浏览器扩展 0.6.0 的合成档案官网安全预演。
- 完整开源文档、测试手册、CI 和公共发布检查。

### Security

- 后台批量发送固定拒绝。
- 合成档案在验证码、官网字段写入和最终提交前停止。
- 扩展提交采用预检、逐岗位授权和官网回执三段式闸门。

## [0.1.0] - 2026-08-03

- 首个 Pydantic AI 简历补全、只读职位搜索、目的地推荐和 dry-run 草稿版本。
