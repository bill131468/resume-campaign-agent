# 浏览器副驾驶安装与安全说明

`browser_extension/` 是 Chrome / Edge Manifest V3 侧边栏插件，当前版本为 0.7.0。它连接 Resume Campaign Agent，并从“投递作战夹”的企业卡片接管对应官网：识别招聘首页、核验同源在招岗位、进入岗位详情和申请页、处理验证码登录、生成填表计划、填写安全字段，并在逐岗位授权后点击唯一的最终提交按钮。

## 安装

1. 启动本项目，确认 `http://127.0.0.1:18010/api/health` 可访问。
2. Chrome 打开 `chrome://extensions`；Edge 打开 `edge://extensions`。
3. 开启“开发者模式”，点击“加载已解压的扩展程序”。
4. 选择本项目中的 `browser_extension` 文件夹，固定“简历投递副驾驶”。
5. 重新加载扩展后回到 `http://127.0.0.1:18010/`，在企业卡片上点击“AI 核岗 · 接管官网”。正式扩展不提供演练跳转按钮。

自动化或开发测试需要先设置 `ENABLE_TEST_FIXTURES=true` 再启动独立测试实例；生产服务默认关闭 `/browser-fixture`、认证 fixture 和模拟回执路由。

第一次接管某个招聘网站时，浏览器会请求该单一域名的权限。确认后扩展打开官网并显示侧边栏；不会同时获得其他招聘网站权限。权限可随时在侧边栏撤销。

合成档案采用“官网安全预演”模式：允许核验官方职位、打开岗位详情并到达登录页或申请页；一旦识别到验证码登录或正式申请表就停止。该模式不会向官网填写手机号、请求验证码、写入简历字段或点击最终提交。只有替换为真实简历后，逐岗位授权投递流程才会启用。

## 工作方式

扫描阶段只读取标签、字段类型、名称、占位符和下拉选项，不读取或上传页面中已有的字段值。Agent 先用确定性规则映射常见字段；不确定的安全字段交给 LLM 做语义映射。LLM 只看到字段结构和“简历中有哪些字段可用”，看不到姓名、电话、邮箱等真实值。模型返回字段映射后，本机服务再绑定真实值。

点击企业卡片的“AI 核岗 · 接管官网”会先在当前官方站点识别职位列表并由 Agent 选择仍在线、Base/方向/资历匹配的岗位；进入认证页后，扩展才把本轮简历手机号填写到官网并点击唯一的获取验证码按钮。手机号不要求用户重复填写。收到短信后，侧栏只弹出验证码输入框；若官网存在隐私/账号协议，用户还必须单独授权。验证码确认按钮同时显示企业、已核验岗位与最终投递说明，只有用户勾选逐岗位授权后才继续。

手机号从本机简历会话按需读取，只在目标官网页面内使用，不写入扩展存储，也不发送给 LLM。验证码只保留在当前侧栏 DOM 内存中，不写入 `chrome.storage`，不发送到本机 Agent、LLM 或日志；关闭弹框或登录跳转后立即清空。扩展不读取系统短信、通知或剪贴板。

## 硬性边界

- 普通填表不填写密码、验证码、证件信息、人口统计信息、详细住址、薪资或同意授权项；验证码仅接受用户在认证弹框中手动输入。
- 不覆盖网页中已经填写的值；填入时只做页面本地空值判断。
- 不自动上传附件，不处理 CAPTCHA，不读取短信，不绕过登录或风控；遇到人机验证立即停止。
- 只有逐岗位授权存在、页面没有 CAPTCHA、缺失必填字段、附件或未确认的人工声明，并且只识别到一个明确的“提交申请/投递”按钮时才允许点击。点击后只报告“已触发”，必须以官网成功回执确认结果。
- 扫描后若页面字段变化，字段签名不一致，旧预检单会被拒绝。
- 简历数据保存在本机 Agent 的内存会话中；扩展持久存储只保存会话 ID，接管任务元数据放在会话级存储且不包含手机号或验证码。

## 受限 Computer Use

`GET /api/browser/capabilities` 声明了 Agent 与插件之间的控制契约。除读取字段结构、AI 映射、标记目标字段、填写空白安全字段外，包含认证页检查、请求官网验证码、登录继续和逐岗位提交四个受控动作。认证与提交都必须由用户在当前企业任务中明确触发。插件不申请 `debugger`、`cookies`、`history` 或 `downloads` 权限，固定站点权限仅包含本机 Agent；招聘网站权限始终是可选且按当前域名单独申请。
